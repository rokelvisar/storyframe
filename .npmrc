# Workspace tooling only. This project is NOT published to an npm registry:
# "publishing" here means the multi-stage Docker image -> ghcr.io (see
# deploy/README.md and docs/deployment.md).
engine-strict=true
save-exact=true

# If a frontend *library* publish is ever wanted, point the scope at GitHub Packages:
# @rokelvisar:registry=https://npm.pkg.github.com
